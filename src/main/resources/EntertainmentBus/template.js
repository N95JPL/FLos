/* eslint-disable no-unused-vars */
import { entertainmentBus } from '../VariableMaps/EntertainmentBusVar'

export function eb520(msg) {
  const strId = msg.id
  const arr = [...msg.data]
}

module.exports = eb520
